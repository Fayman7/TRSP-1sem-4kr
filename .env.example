# Скопируйте в .env и при необходимости измените значения.
# Файл .env не коммитится (см. .gitignore).

# URL базы данных SQLite (по умолчанию — файл products.db в корне проекта)
DATABASE_URL=sqlite:///./products.db
